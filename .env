DATABASE_URL=file:/home/z/my-project/db/custom.db

# Server-side default LLM provider (DeepSeek). Used when the user has NOT
# configured their own custom API in the settings dialog. The key never
# leaves the server — the frontend never sees it.
DEEPSEEK_API_KEY=sk-b891ae78f96a499e84f719075fc9fde8
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
